/**
 * Spring integration utilities.
 *
 * <ul>
 *   <li>{@code SpringChExceptionTranslator} — translates {@code ChException} subtypes
 *       into Spring's {@code DataAccessException} hierarchy</li>
 *   <li>{@code ChDatasource} — qualifier annotation for multi-datasource setups
 *       ({@code @ChDatasource("analytics")})</li>
 * </ul>
 */
package io.blinkhouse.spring.support;
